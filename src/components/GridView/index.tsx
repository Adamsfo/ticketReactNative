import React from "react";
import { View, Text, StyleSheet } from "react-native";
import { Grid, Col, Row } from "react-native-responsive-grid";

export default function GridView() {
  return <Grid></Grid>;
}
